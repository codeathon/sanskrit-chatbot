# Bytecode / caches
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Environment variables (never commit secrets)
.env
.env.*
!.env.example

# Local virtualenv — recreate with: python3 -m venv venv && pip install -r requirements.txt
# This repo may still have venv/ tracked from an older commit; new clones can ignore it here.
venv/
.venv/

# Node (if used)
node_modules/
